# 控制上下文

## 检查下一次请求

继续追问前，点击[节点侧栏](/zh/guides/conversations#浮动侧栏的功能区)输入框上方的 **“将发送约 … tok · … 条消息”** 摘要行。展开的预览会把下一次请求分为材料、显式引用和对话轮次，并显示消息数、文件数与估算 token。它只预览侧栏中的下一次追问，不是独立的画布菜单。

## 建立上下文路径或引用

- 从节点连接点拖到画布空白处，沿结构路径继续提出新问题；
- 从节点连接点拖到已有节点，加入显式引用；
- 在输入框中使用 `@` 提及一个已命名节点。

实线会携带完整的上游对话路径。紫色虚线的摘要引用会带入指定节点的问答和上游问题线索，但不会把每个上游回答都加入请求。

## 改变进入请求的内容

- **删除连线**：移除这条上下文路线，但保留两端节点；
- **归档节点**：保留内容，但把它排除在遍历之外；
- 选中可转换的连线，在**完整结构上下文**与**摘要引用**之间转换。连线操作会显示估算 token 差异。

<img src="../../prune-zh.gif" alt="删除一条进入节点的上下文连线，再重新生成答案" width="100%" loading="lazy"/>

## 验证变化

改变连线后，使用同一个问题和模型重新生成，再[比较前后回答版本](/zh/guides/versions-replay#比较回答版本)。这样可以把上下文干预与提示词或模型变化区分开。

删除连线不等于删除节点：被排除的工作仍留在画布上，可以检查，也可以重新连接到其他位置。
